app:
  host: "0.0.0.0"
  port: 5000
  debug: false

repositories:
  allowed_paths:
    - "/home/user/repos"
    - "/var/git"
  default_path: "/home/user/repos"

analysis:
  default_page_size: 50
  max_page_size: 200
  timeout_seconds: 30

cache:
  enabled: true
  ttl_minutes: 5