[project]
name = "termtimer"
version = "0.1.0"
requires-python = ">=3.11"

[project.dependencies]
textual >= "0.80.0"
notify-run >= "0.2.0"
playsound >= "1.3.0"

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio", "ruff"]

[project.scripts]
termtimer = "termtimer.__main__:main"